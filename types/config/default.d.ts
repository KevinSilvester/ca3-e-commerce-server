export const port: string | number;
export const mongoUri: string;
export namespace cloudinary {
    const name: string;
    const apiKey: string;
    const apiSecret: string;
}
export namespace accessLevel {
    const guest: string;
    const normalUser: string;
    const admin: string;
}
